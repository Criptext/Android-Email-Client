package com.email.websocket

/**
 * WebSocketController needs to interact with the local DB to handle certain commands. This interface
 * abstracts away the implementation of such requests.
 * Created by gabriel on 9/27/17.
 */

/*
TODO()
interface SocketLocalDB : ActivityLocalDB {
    fun saveCriptextAttach(attachment: CriptextAttachment)

    fun getCriptextAttach(fileToken: String): CriptextAttachment?
}*/

interface SocketLocalDB {

}
